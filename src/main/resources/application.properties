spring.application.name=ems-backend
server.port=${PORT:8082}

# Database Configuration
# Railway automatically provides MYSQL_URL, MYSQLUSER, MYSQLPASSWORD
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/ems?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:12345}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# Email Configuration (SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rohanmane259@gmail.com
spring.mail.password=${MAIL_PASSWORD:dklc jqfg xyjt ykcu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID:YOUR_GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET:YOUR_GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=user:email,read:user

# Security Configuration
app.jwt.secret=${JWT_SECRET:your-very-secure-fixed-256-bit-key-for-ems-project-2026-v1}

# Admin Security Configuration
app.admin.email=${ADMIN_EMAIL:admin@gmail.com}
app.admin.master-pin=${ADMIN_MASTER_PIN:789456}
app.admin.default-password=${ADMIN_DEFAULT_PASSWORD:admin123}

# NexGen AI Configuration
app.ai.api-key=${AI_API_KEY:AIzaSyCRLUwQUtihh1DyYgfE7hxvd25BdqcsoKQ}

# Resolve Bean Conflict
spring.main.allow-bean-definition-overriding=true

# Frontend Public URL
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

